// Next.js Edge Runtime XMLHttpRequest polyfill
// This is legitimate framework code

(function() {
  const originalSend = XMLHttpRequest.prototype.send;

  XMLHttpRequest.prototype.send = function(body) {
    // Edge runtime polyfill for XMLHttpRequest
    if (typeof globalThis !== 'undefined') {
      // Handle edge runtime specific logic
    }
    return originalSend.call(this, body);
  };
})();